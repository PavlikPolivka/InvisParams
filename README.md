Goal of this project is to have one small library, that would use invisible UTF characters (zero width characters) as a method to hide some parameters inside http request url.

README under construction